{% set currentRoute = app.request.attributes.get('_route') %}
{% set courrierRoutes = [
    'app_courrier_index',
    'app_courrier_depart_new',
    'app_courrier_new',
    'app_courrier_partenaire_index',
    'app_courrier_recherche',
    'app_courrier_nature_index'
] %}

{% set currentRoute = app.request.attributes.get('_route') %}
{% set currentRoute = app.request.attributes.get('_route') %}
{% set isCourrierActive = currentRoute starts with 'app_courrier_' %}
{% set  sesion_modules = app.session.get('__modules__') %}
{# {% dump(app.session.get('__modules__')) %} #}

<nav class="mt-2">
    <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">

        <li class="nav-item">
            <a data-pjax href="{{ path('dashboard') }}"
               class="nav-link {% if currentRoute starts with 'dashboard' %}active{% endif %}">
                <i class="nav-icon ion-ios-pie"></i>
                <p>Tableau de Bord</p>
            </a>
        </li>
        {% if app.user and is_granted('IS_AUTHENTICATED_FULLY') %}

            {% if is_granted('ROLE_SECRETAIRE') or is_granted('ROLE_ACCUEIL') %}
                {% include('partials_adminlte/_menu_secretaire.html.twig') %}
            {% elseif is_granted('ROLE_LOGISTIQUE') %}
                {% include('partials_adminlte/_menu__logistique.html.twig') %}
            {% elseif is_granted('ROLE_ADMIN') %}
                {% include('partials_adminlte/_menu_admin.html.twig') %}
            {% elseif is_granted('ROLE_SUPER_ADMIN') %}
                {% include('partials_adminlte/_menu_super_admin.html.twig') %}
            {% endif %}
        {% endif %}


    </ul>
</nav>
