{% extends 'base.html.twig' %}

{% block title %}Modifier un Type Maintenance{% endblock %}

{% block breadcrumb %}TYPES MAINTENANCES > MODIFIER{% endblock %}

{% block content %}

    <div class="row">
        <div class="col-12">
            <h4>Modifier le Type de Maintenance</h4>
            <div class="card my-4">
                <div class="card-body">
                    {{ form_start(form) }}
                    
                    <div class="input-group input-group-static mb-4">
                        {{ form_label(form.libelle, 'Nom du Type de Maintenance', { 'label_attr': { 'class': 'ms-0' } }) }}
                        {{ form_widget(form.libelle, { 'attr': { 'class': 'form-control' } }) }}
                        {{ form_errors(form.libelle) }}
                    </div>

                    <button type="submit" class="btn btn-primary">Mettre à Jour</button>
                    <a href="{{ path('types_maitenances_index') }}" class="btn btn-secondary">Annuler</a>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
    
{% endblock %}