{% extends 'base.html.twig' %}

{% block title %}Caisse{% endblock %}

{% block body %}
    <h1>Caisse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caisse.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ caisse.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_emploie_caisse_index') }}">back to list</a>

    <a href="{{ path('app_emploie_caisse_edit', {'id': caisse.id}) }}">edit</a>

    {{ include('emploie/caisse/_delete_form.html.twig') }}
{% endblock %}
