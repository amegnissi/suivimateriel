{% extends 'base.html.twig' %}

{% block title %}Exercice{% endblock %}

{% block body %}
    <h1>Exercice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exercice.id }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ exercice.annee }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_emploie_exercice_index') }}">back to list</a>

    <a href="{{ path('app_emploie_exercice_edit', {'id': exercice.id}) }}">edit</a>

    {{ include('emploie/exercice/_delete_form.html.twig') }}
{% endblock %}
