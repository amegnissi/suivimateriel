<form method="post" action="{{ path('app_courrier_nature_courrier_delete', {'id': nature_courrier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nature_courrier.id) }}">
    <button class="btn">Delete</button>
</form>
